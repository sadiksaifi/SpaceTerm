# Let OpenSSH own remote authentication

Remote Project Workspaces use OpenSSH authentication, configured destinations, and host
verification so user SSH configuration, credentials, and trust policy remain consistent. SpaceTerm
owns Control Connection and Terminal Session Channel lifecycles and presents Authentication Prompts.
